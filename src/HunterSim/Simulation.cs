namespace HunterSim
{
    public class Simulation
    {
        private SimulationConfig _config;

        public Simulation(SimulationConfig config)
        {
            _config = config;
        }

        public double Run()
        {
            return 0.0;
        }
    }
}
